<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:activiti="http://activiti.org/bpmn"
             xmlns:bpmndi="http://www.omg.org/spec/BPMN/20100524/DI"
             xmlns:omgdc="http://www.omg.org/spec/DD/20100524/DC"
             xmlns:omgdi="http://www.omg.org/spec/DD/20100524/DI"
             typeLanguage="http://www.w3.org/2001/XMLSchema"
             expressionLanguage="http://www.w3.org/1999/XPath"
             targetNamespace="http://www.club.venue.com/activity">

    <process id="activity_approval" name="活动审批流程" isExecutable="true">

        <!-- 开始事件 -->
        <startEvent id="startEvent" name="开始"/>

        <!-- 提交活动申请（由startProcess自动完成） -->
        <userTask id="submitTask" name="提交活动申请"
                  activiti:assignee="applicant"/>

        <!-- 一级审批（管理员） -->
        <userTask id="firstReview" name="一级审批"
                  activiti:assignee="admin"/>

        <!-- 排他网关：根据活动时长判断是否需要二级审批 -->
        <exclusiveGateway id="durationGateway" name="时长判断"/>

        <!-- 二级审批（超级管理员，仅活动时长>48小时触发） -->
        <userTask id="secondReview" name="二级审批"
                  activiti:assignee="super_admin"/>

        <!-- 结束事件 -->
        <endEvent id="endEvent" name="结束"/>

        <!-- 流程连线 -->
        <sequenceFlow id="flow1" sourceRef="startEvent" targetRef="submitTask"/>
        <sequenceFlow id="flow2" sourceRef="submitTask" targetRef="firstReview"/>
        <sequenceFlow id="flow3" sourceRef="firstReview" targetRef="durationGateway"/>

        <!-- 网关分支：时长>48小时 → 二级审批 -->
        <sequenceFlow id="flow4" sourceRef="durationGateway" targetRef="secondReview">
            <conditionExpression xsi:type="tFormalExpression">
                ${durationHours != null &amp;&amp; durationHours > 48}
            </conditionExpression>
        </sequenceFlow>

        <!-- 网关分支：时长≤48小时 → 直接结束 -->
        <sequenceFlow id="flow5" sourceRef="durationGateway" targetRef="endEvent">
            <conditionExpression xsi:type="tFormalExpression">
                ${durationHours == null || durationHours &lt;= 48}
            </conditionExpression>
        </sequenceFlow>

        <sequenceFlow id="flow6" sourceRef="secondReview" targetRef="endEvent"/>

    </process>

</definitions>
